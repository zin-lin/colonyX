# Virtual Ant Farm 
- using `docker`
- using `OOP`
- using `python`
- using `React`
- using `everything I can `

# Building 
- `docker-compose`
  - ```shell
    docker-compose up --build
    ``` 
- `electron`
  - ```shell
    cd entry
    npm start
    ```
